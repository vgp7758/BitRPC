// Generated by BitRPC Protocol Generator
// File: GetUserResponse_serializer.cpp
// Language: Cpp
// Date: 2025-09-11 11:50:09


#include "example::protocol/getuserresponse_serializer.h"

namespace bitrpc {
namespace example::protocol {

int GetUserResponseSerializer::hash_code() const {
    return static_cast<int>(std::hash<std::string>{}("GetUserResponse"));
}

void GetUserResponseSerializer::write(const void* obj, StreamWriter& writer) const {
    const auto& message = *static_cast<const GetUserResponse*>(obj);
    BitMask mask;

    // Bit mask group 0
    mask.set_bit(0, !is_default_struct(message.user));
    mask.set_bit(1, !is_default_bool(message.found));
    mask.write(writer);

    // Write field values
    if (mask.get_bit(0)) {
        writer.write_object(message.user);
    }
    if (mask.get_bit(1)) {
        writer.write_bool(message.found);
    }
}

void* GetUserResponseSerializer::read(StreamReader& reader) const {
    auto message = std::make_unique<GetUserResponse>();

    // Read bit mask group 0
    BitMask mask0;
    mask0.read(reader);

    if (mask0.get_bit(0)) {
        message->user = reader.read_object();
    }
    if (mask0.get_bit(1)) {
        message->found = reader.read_bool();
    }
    return message.release();
}

    bool is_default_struct(const void*& value) const;
    bool is_default_bool(const bool& value) const;

bool GetUserResponseSerializer::is_default_struct(const void*& value) const {
    return value == ;
}
bool GetUserResponseSerializer::is_default_bool(const bool& value) const {
    return value == false;
}

}} // namespace bitrpc
