// Generated by BitRPC Protocol Generator
// File: LoginRequest_serializer.cpp
// Language: Cpp
// Date: 2025-09-11 11:50:09


#include "example::protocol/loginrequest_serializer.h"

namespace bitrpc {
namespace example::protocol {

int LoginRequestSerializer::hash_code() const {
    return static_cast<int>(std::hash<std::string>{}("LoginRequest"));
}

void LoginRequestSerializer::write(const void* obj, StreamWriter& writer) const {
    const auto& message = *static_cast<const LoginRequest*>(obj);
    BitMask mask;

    // Bit mask group 0
    mask.set_bit(0, !is_default_string(message.username));
    mask.set_bit(1, !is_default_string(message.password));
    mask.write(writer);

    // Write field values
    if (mask.get_bit(0)) {
        writer.write_string(message.username);
    }
    if (mask.get_bit(1)) {
        writer.write_string(message.password);
    }
}

void* LoginRequestSerializer::read(StreamReader& reader) const {
    auto message = std::make_unique<LoginRequest>();

    // Read bit mask group 0
    BitMask mask0;
    mask0.read(reader);

    if (mask0.get_bit(0)) {
        message->username = reader.read_string();
    }
    if (mask0.get_bit(1)) {
        message->password = reader.read_string();
    }
    return message.release();
}

    bool is_default_string(const std::string& value) const;

bool LoginRequestSerializer::is_default_string(const std::string& value) const {
    return value == "";
}

}} // namespace bitrpc
