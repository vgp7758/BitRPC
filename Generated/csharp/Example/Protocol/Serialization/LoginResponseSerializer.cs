// Generated by BitRPC Protocol Generator
// File: LoginResponseSerializer.cs
// Language: CSharp
// Date: 2025-09-11 11:50:09


using System;
using System.IO;
using BitRPC.Serialization;

namespace Example.Protocol.Serialization
{
    public class LoginResponseSerializer : ITypeHandler
    {
        public int HashCode => typeof(LoginResponse).GetHashCode();

        public void Write(object obj, BitRPC.Serialization.StreamWriter writer)
        {
            var message = (LoginResponse)obj;
            BitRPC.Serialization.BitMask mask = null;
            try
            {
                mask = BitRPC.Serialization.BitMaskPool.Get(1);
            // Bit mask group 0
            mask.SetBit(0, !IsDefault(message.success));
            mask.SetBit(1, !IsDefault(message.user));
            mask.SetBit(2, !IsDefault(message.token));
            mask.SetBit(3, !IsDefault(message.error_message));
            mask.Write(writer);

            // Write field values
            if (mask.GetBit(0))
            {
                writer.WriteBool(message.success);
            }
            if (mask.GetBit(1))
            {
                UserSerializer.Write(message.user, writer);
            }
            if (mask.GetBit(2))
            {
                writer.WriteString(message.token);
            }
            if (mask.GetBit(3))
            {
                writer.WriteString(message.error_message);
            }
            }
            finally
            {
                if (mask != null)
                {
                    BitRPC.Serialization.BitMaskPool.Return(mask);
                }
            }
        }

        public object Read(BitRPC.Serialization.StreamReader reader)
        {
            var message = new LoginResponse();

            // Read bit masks using object pool
            BitRPC.Serialization.BitMask[] masks = new BitRPC.Serialization.BitMask[1];
            try
            {
            // Read bit mask group 0
            masks[0] = BitRPC.Serialization.BitMaskPool.Get(1);
            masks[0].Read(reader);
            if (masks[0].GetBit(0))
            {
                message.success = reader.ReadBool();
            }
            if (masks[0].GetBit(1))
            {
                message.user = UserSerializer.ReadStatic(reader);
            }
            if (masks[0].GetBit(2))
            {
                message.token = reader.ReadString();
            }
            if (masks[0].GetBit(3))
            {
                message.error_message = reader.ReadString();
            }
            return message;
            }
            finally
            {
                foreach (var mask in masks)
                {
                    if (mask != null) BitRPC.Serialization.BitMaskPool.Return(mask);
                }
            }
        }

        private bool IsDefault(bool value)
        {
            return value == false;
        }

        private bool IsDefault(User value)
        {
            return value == default;
        }

        private bool IsDefault(string value)
        {
            return string.IsNullOrEmpty(value);
        }

        private static readonly LoginResponseSerializer _instance = new LoginResponseSerializer();

        public static void Write(LoginResponse obj, BitRPC.Serialization.StreamWriter writer)
        {
            _instance.Write(obj, writer);
        }

        public static LoginResponse ReadStatic(BitRPC.Serialization.StreamReader reader)
        {
            return (LoginResponse)_instance.Read(reader);
        }

    }
}
