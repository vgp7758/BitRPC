// Generated by BitRPC Protocol Generator
// File: GetUserResponseSerializer.cs
// Language: CSharp

using System;
using System.IO;
using BitRPC.Serialization;
using System.Collections.Generic;

namespace Example.Protocol.Serialization
{
    public class GetUserResponseSerializer : ITypeHandler
    {
        public int HashCode => -1624387005;

        public void Write(object obj, BitRPC.Serialization.StreamWriter writer)
        {
            var message = (GetUserResponse)obj;
            BitRPC.Serialization.BitMask mask = null;
            try
            {
                mask = BitRPC.Serialization.BitMaskPool.Get(1);
            // Bit mask group 0
            mask.SetBit(0, !IsDefault(message.user));
            mask.SetBit(1, !IsDefault(message.found));
            mask.Write(writer);

            // Write field values
            if (mask.GetBit(0))
            {
                UserInfoSerializer.Write(message.user, writer);
            }
            if (mask.GetBit(1))
            {
                writer.WriteBool(message.found);
            }
            }
            finally
            {
                if (mask != null)
                {
                    BitRPC.Serialization.BitMaskPool.Return(mask);
                }
            }
        }

        public object Read(BitRPC.Serialization.StreamReader reader)
        {
            var message = new GetUserResponse();

            // Read bit masks using object pool
            BitRPC.Serialization.BitMask[] masks = new BitRPC.Serialization.BitMask[1];
            try
            {
            // Read bit mask group 0
            masks[0] = BitRPC.Serialization.BitMaskPool.Get(1);
            masks[0].Read(reader);
            if (masks[0].GetBit(0))
            {
                message.user = UserInfoSerializer.ReadStatic(reader);
            }
            if (masks[0].GetBit(1))
            {
                message.found = reader.ReadBool();
            }
            return message;
            }
            finally
            {
                foreach (var mask in masks)
                {
                    if (mask != null) BitRPC.Serialization.BitMaskPool.Return(mask);
                }
            }
        }

        private bool IsDefault(UserInfo value)
        {
            return value == default;
        }

        private bool IsDefault(bool value)
        {
            return value == false;
        }

        private static readonly GetUserResponseSerializer _instance = new GetUserResponseSerializer();

        public static void Write(GetUserResponse obj, BitRPC.Serialization.StreamWriter writer)
        {
            _instance.Write(obj, writer);
        }

        public static GetUserResponse ReadStatic(BitRPC.Serialization.StreamReader reader)
        {
            return (GetUserResponse)_instance.Read(reader);
        }

    }
}
