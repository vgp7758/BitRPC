// Test Protocol for BitRPC
namespace Example.Protocol

message LoginRequest {
    string username = 1;
    string password = 2;
}

message LoginResponse {
    bool success = 1;
    string message = 2;
}

service AuthService {
    LoginResponse Login(LoginRequest request);
}